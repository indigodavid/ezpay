<!DOCTYPE html>
<html>
  <head>
    <title>Ezpay</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://kit.fontawesome.com/83d0b64e5b.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Pacifico&display=swap" rel="stylesheet">
  </head>

  <body class="bg-stone-200 flex flex-col">
    <%= render 'shared/navbar' unless current_page?(root_path) || current_page?(splash_index_path)%>

    <main class="container mx-auto flex-grow flex flex-col">
      <% flash.each do |type, msg| %>
        <div class="box-alert <%= type %>" role="alert">
          <span class="font-medium capitalize"><%= type %>: </span><%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
